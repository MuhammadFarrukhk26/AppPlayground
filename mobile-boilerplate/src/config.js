// IMPORTANT: 'localhost' only works in the iOS Simulator. For a real physical device
// (Expo Go on your phone) or an Android emulator, you MUST use your computer's LAN IP
// address instead — e.g. http://192.168.1.42:4000 — because the phone is a separate
// device on the network and has no concept of "localhost" pointing at your laptop.
//
// Find your LAN IP:
//   Mac/Linux: ifconfig | grep "inet "
//   Windows:   ipconfig
//
// Android Emulator only (not a real device): use 10.0.2.2 instead of your LAN IP.

export const API_BASE_URL = 'https://haazir-backend-nine.vercel.app/api/';
export const SOCKET_URL = 'https://haazir-backend-nine.vercel.app/socket';